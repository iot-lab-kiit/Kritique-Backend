<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Faculty Details</title>
        <style>
            body {
                font-family: Arial, sans-serif;
                margin: 0;
                padding: 0;
                background-color: #f4f4f4;
            }

            .container {
                max-width: 800px;
                margin: 20px auto;
                padding: 20px;
                background-color: #fff;
                border-radius: 5px;
                box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            }

            h1 {
                color: #333;
                margin-bottom: 20px;
            }

            p {
                color: #333;
                margin-bottom: 10px;
                text-align: left; /* Align text to the left within the paragraphs */
            }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>Faculty Details:</h1>
            <p>_id: <%= faculty._id %></p>
            <p>Name: <%= faculty.name %></p>
            <p>Experience: <%= faculty.experience %> years</p>
            <p>PhotoURL: <%= faculty.photoUrl %></p>
            <p>Average Rating: <%= faculty.avgRating %></p>
            <p>Total Rating: <%= faculty.totalRating %></p>
        </div>
    </body>
</html>
